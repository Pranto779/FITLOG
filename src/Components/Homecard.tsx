import React from 'react';
const GetData=async()=>{
    const res=await fetch('')
}

const Homecard = () => {
    return (
        <div>
            
        </div>
    );
};

export default Homecard;